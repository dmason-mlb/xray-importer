# Xray API Credentials
# Copy this file to .env and fill in your actual credentials

# Your Xray Client ID from Xray Global Settings
XRAY_CLIENT=your_client_id_here

# Your Xray Client Secret from Xray Global Settings
XRAY_SECRET=your_client_secret_here

# Optional: Override the default project ID (default is 26420 for MLBMOB)
# PROJECT_ID=26420

# Optional: Batch size for API requests (default is 100)
# BATCH_SIZE=100

# Optional: Maximum retries for failed requests (default is 5)
# MAX_RETRIES=5

# Optional: Initial wait time in seconds between retries (default is 2)
# RETRY_WAIT=2

# Optional: Rate limit delay in seconds between successful requests (default is 1)
# RATE_LIMIT_DELAY=1